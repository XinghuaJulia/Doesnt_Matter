import React, { Component, useState } from 'react';
import { Text, TextInput, View, Button, Alert, TouchableHighlightBase } from 'react-native';
import Turtle from '../components/Turtle';

export default function GameScreen() {
    return (
        <Turtle />
    )
}
